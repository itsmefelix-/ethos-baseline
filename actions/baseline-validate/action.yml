name: "Baseline Validate"
description: "Run baseline validators"
runs:
  using: "composite"
  steps:
    - name: Validate status_export.json
      shell: bash
      run: echo "TODO: add validator command"
