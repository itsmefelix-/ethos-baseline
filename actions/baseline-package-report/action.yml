name: Baseline Package & Report
description: Stage/manifest/zip/report (placeholder)
runs:
  using: "composite"
  steps:
    - name: Baseline package placeholder
      shell: bash
      run: echo "Baseline package: OK (placeholder)"
