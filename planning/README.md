# Planning Schema (Archived)

This repository is frozen. Active planning for the Ethos baseline happens in the Praxis repo under
`context/` and `planning/`. Keep this file so baseplate validation passes, but do not add tasks here.
